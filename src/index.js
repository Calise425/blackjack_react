import React, { useState } from "react";
import ReactDOM from "react-dom";

const BlackJack = () => {
  return <h1>Hello World</h1>;
};

ReactDOM.render(<BlackJack />, document.getElementById("app"));
